# Exemplo de variáveis de ambiente necessárias para o projeto.
# Copie este arquivo para .env e preencha com os valores corretos.

# UID do usuário no Linux/macOS para evitar problemas de permissão.
# Rode `id -u` no terminal para obter o seu.
AIRFLOW_UID=50000